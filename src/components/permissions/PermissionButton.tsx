import type React from "react";
import type { PermissionButtonProps } from "@/core/types/permissions";

// Stub - permissions not used, always render children
export const PermissionButton: React.FC<PermissionButtonProps> = ({ children }) => {
	return children;
};
